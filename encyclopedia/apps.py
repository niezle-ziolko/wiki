from django.apps import AppConfig
# Import AppConfig from django.apps to define a configuration class for the 'encyclopedia' app

class EncyclopediaConfig(AppConfig):
    name = 'encyclopedia'
    # The name attribute specifies the name of the app, which is 'encyclopedia' in this case